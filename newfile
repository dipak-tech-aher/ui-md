<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Font Size Slider</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            display: flex;
            align-items: center;
        }

        .label {
            font-size: 16px;
            margin-right: 10px;
            font-weight: bold;
        }

        .font-label {
            font-size: 16px;
            margin: 0 5px;
            font-weight: bold;
        }

        .slider-container {
            position: relative;
            width: 150px;
            height: 5px;
            background-color: #ccc;
            border-radius: 5px;
        }

        .slider-container::before {
            content: '';
            position: absolute;
            top: -3px;
            left: 0;
            width: 100%;
            height: 11px;
            background: repeating-linear-gradient(to right, 
                #000 0%, 
                #000 1px, 
                transparent 1px, 
                transparent calc((100% / 5) - 1px)
            );
        }

        .slider {
            position: absolute;
            top: -7px;
            left: 0;
            width: 100%;
            -webkit-appearance: none;
            appearance: none;
            background: none;
            outline: none;
            margin: 0;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 15px;
            height: 15px;
            background: #000;
            border-radius: 50%;
            cursor: pointer;
            position: relative;
            top: 2px; /* Adjusted to align the thumb with the track */
        }

        .slider::-moz-range-thumb {
            width: 15px;
            height: 15px;
            background: #000;
            border-radius: 50%;
            cursor: pointer;
            position: relative;
            top: 2px; /* Adjusted to align the thumb with the track */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="label">Font Size</div>
        <div class="font-label">A</div>
        <div class="slider-container">
            <input type="range" min="10" max="36" value="16" step="5.2" class="slider" id="fontSizeSlider">
        </div>
        <div class="font-label">A</div>
    </div>

    <script>
        // const slider = document.getElementById('fontSizeSlider');
        // const body = document.body;

        // slider.addEventListener('input', function() {
        //     body.style.fontSize = slider.value + 'px';
        // });
    </script>
</body>
</html>
