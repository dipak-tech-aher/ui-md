<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* Container for the toggle switch */
.toggle-switch {
  display: flex;
  align-items: center;
}

/* Hide the checkbox input */
.toggle-input {
  display: none;
}

/* The label will be the toggle button */
.toggle-label {
  width: 40px;
  height: 20px;
  background-color: #88C0D0; /* Background color similar to the image */
  border-radius: 20px;
  position: relative;
  cursor: pointer;
  margin-right: 10px;
  transition: background-color 0.3s ease;
}

/* Circle inside the switch */
.toggle-label::after {
  content: '';
  position: absolute;
  top: 2px;
  left: 2px;
  width: 16px;
  height: 16px;
  background-color: white;
  border-radius: 50%;
  transition: transform 0.3s ease;
}

/* Move the circle when the toggle is checked */
.toggle-input:checked + .toggle-label::after {
  transform: translateX(20px);
}

/* Change the background color when checked */
.toggle-input:checked + .toggle-label {
  background-color: #5E81AC; /* Adjust the color if needed */
}

/* Text beside the toggle */
.label-text {
  font-family: Arial, sans-serif;
  font-size: 14px;
  color: #4A4A4A; /* Adjust the text color */
}

    </style>
</head>
<body>
    <div class="toggle-switch">
        <input type="checkbox" id="signatureToggle" class="toggle-input">
        <label for="signatureToggle" class="toggle-label"></label>
        <span class="label-text">Signature</span>
      </div>
      
</body>
</html>
